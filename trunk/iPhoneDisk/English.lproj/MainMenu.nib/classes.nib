{
    IBClasses = (
        {
            ACTIONS = {button = id; mediaPressed = id; rootPressed = id; }; 
            CLASS = DiskController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                button = NSButton; 
                media = NSButtonCell; 
                progress = NSProgressIndicator; 
                radio = NSMatrix; 
                root = NSButtonCell; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {CLASS = NSLevelIndicator; LANGUAGE = ObjC; SUPERCLASS = NSControl; }
    ); 
    IBVersion = 1; 
}